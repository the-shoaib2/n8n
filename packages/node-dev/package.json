{
  "name": "aura-node-dev",
  "version": "1.118.0",
  "description": "CLI to simplify aura credentials/node development",
  "main": "dist/src/index",
  "types": "dist/src/index.d.ts",
  "oclif": {
    "commands": "./dist/commands",
    "bin": "aura-node-dev"
  },
  "scripts": {
    "clean": "rimraf dist .turbo",
    "dev": "pnpm watch",
    "build": "tsc --noEmit",
    "build-node-dev": "tsc",
    "format": "biome format --write .",
    "format:check": "biome ci .",
    "lint": "eslint src --quiet",
    "lint:fix": "eslint src --fix",
    "prepack": "echo \"Building project...\" && rm -rf dist && tsc -b",
    "watch": "tsc --watch"
  },
  "bin": {
    "aura-node-dev": "./bin/aura-node-dev"
  },
  "keywords": [
    "development",
    "node",
    "helper",
    "aura"
  ],
  "files": [
    "bin",
    "dist",
    "templates",
    "src/tsconfig-build.json"
  ],
  "devDependencies": {
    "@aura/typescript-config": "workspace:*",
    "@types/inquirer": "^6.5.0"
  },
  "dependencies": {
    "@aura/di": "workspace:*",
    "@oclif/core": "4.0.7",
    "change-case": "^4.1.1",
    "fast-glob": "catalog:",
    "inquirer": "^7.0.1",
    "aura-core": "workspace:*",
    "aura-workflow": "workspace:*",
    "replace-in-file": "^6.0.0",
    "tmp-promise": "^3.0.3"
  }
}
